# 终生成长知识库 · 使用说明

## 一、系统简介

终生成长知识库是一套面向个人中长期知识储备的 Web 系统，聚焦国内政策、宏观经济、产业、金融监管、前沿科技、全球地缘等重大事件。

核心能力：
- 每日新闻采集与认知拓展候选话题
- 7 天专题订阅 / 长期跟踪
- 多用户分发（owner / member / trial）
- 手机 + 电脑双向同步
- 全文检索、相关卡片推荐、书签收藏、版本快照

---

## 二、快速启动

### 1. 配置 Supabase

1. 在 [Supabase](https://supabase.com/) 创建免费项目。
2. 进入 Project Settings -> API，复制：
   - Project URL
   - anon public key
   - service_role key（**仅本地脚本使用，不要写入前端**）
3. 开启 Authentication -> Email。

### 2. 运行数据库 SQL

在 Supabase SQL Editor 按顺序执行：

```sql
\i supabase_sql/01_schema.sql
\i supabase_sql/02_rls.sql
\i supabase_sql/03_functions.sql
```

### 3. 配置本地环境

```bash
cp .env.example .env
```

填写 `.env`：

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### 4. 安装依赖

```bash
# Python
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt

# 前端
cd frontend
npm install
npm run dev
```

### 5. 创建第一个 owner 账号

```bash
python scripts/create_trial_user.py --email your@email.com --password yourpassword
```

然后到 Supabase Dashboard -> Table Editor -> `user_profile`，把该用户的 `user_role` 改为 `owner`。

---

## 三、角色与权限

| 角色 | 权限 |
|------|------|
| **owner** | 全部读写，可进入管理后台 |
| **member** | 只读业务数据，可写个人备注和书签 |
| **trial** | 只读业务数据，7 天后过期被数据库层拦截 |
| 匿名 | 禁止访问 |

---

## 四、前端页面说明

### 1. 登录页

- 使用 Supabase Auth 邮箱/密码登录。
- trial 账号过期后自动跳转“试用已到期”页面。

### 2. 今日情报（首页）

- **今日重点**：顶部折叠面板，展示当天最重要的前 3 条新闻摘要。
- **新闻列表**：默认展开前 3 条，其余可“展开全文”。
- **已读/未读**：未读新闻标题旁有蓝色小圆点，顶部显示“已读 X/Y”。
- **收藏**：点击 ☆ / ★ 收藏新闻。
- **查看原文**：点击跳转外部原文链接，同时记录浏览日志。
- **认知拓展候选**：系统推荐的待跟踪话题，可点击“同意订阅”开启 7 天专题，或“跳过”。

### 3. 知识库

- **分类筛选**：11 大一级分类下拉选择。
- **标签筛选**：逗号分隔多个标签。
- **全文检索**：输入关键词，搜索卡片内容、分类、标签。
- **个人备注**：每张卡片可写独立备注，仅自己可见。
- **收藏**：点击 ☆ / ★ 收藏卡片。
- **相关卡片**：卡片详情下方自动推荐标签相近的其他卡片。
- **历史版本**：owner 可查看卡片修改历史（仅 owner）。

### 4. 搜索页

- 全局搜索新闻 + 知识卡片。
- 结果混合展示，显示匹配度和原文链接。

### 5. 订阅专题

- 展示活跃和归档的订阅任务。
- 7 天专题带进度条和 1-7 天日程条。
- 到期后 owner 可选择：**永续 / 续 7 天 / 归档 / 删除**。
- 长期跟踪专题无 7 天限制，owner 可手动归档/删除。

### 6. 管理后台（仅 owner）

- **仪表盘**：总新闻、今日新闻、知识卡片、活跃用户、活跃订阅等核心指标。
- **创建试用账号**：填写邮箱密码后生成 CLI 命令，在本机终端执行创建。
- **用户管理**：修改角色、状态、试用到期时间、订阅包。
- **登录审计**：查看所有用户登录时间与设备信息。
- **浏览日志**：查看最近 50 条用户浏览行为。

---

## 五、本地脚本使用

### 每日采集

```bash
# 运行全部采集源（本地 JSON + RSS）
python scripts/collect_daily.py

# 仅本地输入
python scripts/collect_daily.py --source local

# 仅 RSS
python scripts/collect_daily.py --source rss

# 只预览不写入
python scripts/collect_daily.py --dry-run
```

### 创建试用账号

```bash
python scripts/create_trial_user.py --email user@example.com --password xxxxxx --packages 包A 包B
```

### 备份与日志清理

```bash
# 全库备份 + 清理 90 天前浏览日志 + 每周备份提醒
python scripts/backup_db.py

# 仅清理日志
python scripts/backup_db.py --clean-only
```

建议用 Windows 任务计划程序 / Linux cron 每周自动运行一次。

---

## 六、数据结构说明

主要表：

| 表名 | 用途 |
|------|------|
| `daily_news` | 每日新闻 |
| `knowledge_card` | 知识卡片 |
| `knowledge_card_history` | 卡片版本快照 |
| `user_card_note` | 用户个人备注（按用户隔离） |
| `user_news_read` | 用户新闻阅读状态（按用户隔离） |
| `user_bookmark` | 用户书签/收藏（按用户隔离） |
| `explore_candidate` | 认知拓展候选话题 |
| `subscribe_task` | 订阅专题任务 |
| `subscribe_package` | 订阅包 / 可见范围 |
| `user_profile` | 用户业务角色与订阅包 |
| `login_log` | 登录审计 |
| `browse_log` | 内容浏览日志 |
| `operation_log` | 关键操作日志（可选） |

---

## 七、PWA 与深色模式

### 添加到手机桌面

1. 用手机浏览器打开部署后的网址。
2. 浏览器菜单选择“添加到主屏幕”/“添加到桌面”。
3. 即可像 App 一样全屏打开。

### 深色模式

- 点击顶部导航栏的 ☾ / ☀ 按钮切换。
- 偏好会自动保存在浏览器本地。

---

## 八、常见问题

**Q：试用账号过期后数据还在吗？**  
A：在。账号过期只是被前端和数据库层拦截访问，owner 可在后台延长有效期或升级角色。

**Q：为什么 admin 后台不能直接创建用户？**  
A：前端只持有 anon key，创建 Auth 用户需要 service_role_key。点击“生成创建命令”后，在本机终端执行即可。

**Q：图片怎么查看？**  
A：系统不存储图片二进制，只保存原文 URL。点击“查看原文”会新标签页打开外部网页。

**Q：订阅专题最多几个？**  
A：同时最多 3 个活跃订阅。

**Q：全文检索支持中文吗？**  
A：支持。基于 PostgreSQL `pg_trgm` 扩展，支持中文子串匹配、分类和标签匹配。

---

## 九、后续可扩展

- PWA 离线缓存已读内容
- AI 问答入口（二期 RAG）
- 分类目录树视图
- 死链自动检测
